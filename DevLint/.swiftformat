# DevLint SwiftFormat Rules

# General Rules
indent: 4 # Use 4 spaces for indentation
linebreaks: lf # Use LF (Unix-style) line endings
maxwidth: 100 # Wrap lines that exceed 100 characters

# Whitespace & Operators
spacearoundoperators: true # Ensures spaces around operators (e.g., `a + b`)
redundantSelf: remove # Removes redundant `self.` where not needed
redundantGet: remove # Removes `get {}` in computed properties

# Braces & Control Flow
braces: same-line # Keep `{` on the same line as function/class declarations
strongOutlets: true # Converts `if let` to `guard let` when appropriate

# Imports & Redundancies
redundantImport: remove # Removes unused `import` statements
trailingCommas: always # Enforces trailing commas in multiline lists

# Comments
comments: indent # Ensures comments align properly

# Miscellaneous
redundantParens: remove # Removes unnecessary parentheses
emptyClosures: spaced # `{}` → `{ }` for better readability
